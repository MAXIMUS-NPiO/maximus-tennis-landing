# RELEASE CHECKLIST — maximus.tennis v4.2 (branch `release/ads-readiness-v4.2`)

Run date 2026-09-22 (final build of this branch). Environment: production build (`next build` + `next start`) with a production-mode local stack — real `redis-server` behind an Upstash-compatible REST bridge, SMTP sink with implicit TLS and AUTH (self-signed certificate trusted via `NODE_EXTRA_CA_CERTS`). Browser: Chromium via Playwright — **emulated** viewports, offline mode, zoom and reduced motion; **no physical devices, no mainland-China network**. Scripts: `tests/intake.test.mjs`, `tests/e2e/acceptance.mjs`, `tests/e2e/consent.mjs`, `scripts/crawl.mjs`.

| # | Scenario | Check | Result | Evidence | Dependency for production |
| --- | --- | --- | --- | --- | --- |
| 1 | GREAT, POWER, SPIN open the configurator with the correct series | "Configure this series" on each series page → series preselected, flow starts at the precision step, URL parameters consumed | PASS | S01: great/power/spin preselected, step "02 Precision class", URL `/en/build` | — |
| 2 | Invalid weight: UI error and server block | Custom weight 0, −5, abc, 250.5 → field error, stays on step; server POST with weight 0 → 400 `config.weight: out_of_range`; POWER + 272 g → 400 `not_in_matrix` | PASS | S02; unit tests "strict integers", "listed weight", "custom weight" | — |
| 3 | Series change leaves no stale values | POWER 210 g → switch to GREAT → notice "210 g is not in the GREAT matrix…", weight cleared in state and UI | PASS | S03 | — |
| 4 | Precision shows four parameters; matched-set quantity | Every class label shows g / mm / kg·cm² / RA (P0.5: "conforms to the declared target RA under the factory protocol"); triple × 2 = 6, team 2 × 6 = 12, team size 3 rejected | PASS | S04 | — |
| 5 | Configuration request accepted end-to-end | Full configurator → contact → 201, ID `MX-CFG-…`; stored record = series great, P1.5, 272 g, L3, triple × 2, total 6, entry "series" | PASS | S05, screenshots `configurator-review`, `configurator-success` | Store (D1) |
| 6 | GPS reaches the form without losing answers; seed applied once | Full GPS → profile submitted (`MX-GPS-…`, stored with free text and directions); session storage holds enum answers only; "Configure GREAT" applies series + grip once; user's later choice (POWER) survives reload; padel → no tennis series | PASS | S06 | Store (D1) |
| 7 | UTM landing → language switch → accepted request with allowed attribution | `/en/racquets/great?utm_…&gclid=…` → RU switch keeps UTM, drops gclid (no consent); `/ru/contact?purpose=coach` → EN keeps purpose; `/ru/choose` request stores landing_path `/en/racquets/great`, locale, utm_source/medium/campaign | PASS | S07 | Store (D1) |
| 8 | Family and coach (and distribution) forms use the correct purpose | IDs `MX-FAM-…`, `MX-COA-…`, `MX-DST-…`; stored purpose and entry context match | PASS | S08 | Store (D1) |
| 9 | Double submit creates one request | Double click → one record; replay of the same key + payload → same ID with `duplicate: true` | PASS | S09; unit "double submit", "redis store: atomic idempotent create" | Store (D1) |
| 10 | Offline: no false success, input kept | Offline submit → "Your request has not been submitted… device is offline", no ID, input kept; back online → retry accepted | PASS | S10, screenshot `form-offline-failed` | — |
| 11 | Save error: no false success, input kept; one request after recovery | Store bridge stopped → 503 "Requests cannot be accepted at the moment", input kept; restarted → retry accepted, exactly +1 record | PASS | S11, screenshot `form-store-unavailable` | — |
| 12 | Notification error: request stays saved, retry scheduled | SMTP down → request accepted and stored; notification `retry`, `smtp_connection`, next attempt +1 min; SMTP up → manual retry → `sent`, message ID recorded, plain-text mail with Reply-To; cross-origin retry POST → 403 | PASS | S12; unit "retry schedule" (8 attempts → failed, record kept), "webhook", "SMTP" | Notification channel (D2), CRON_SECRET (D3) |
| 13 | Analytics opt-out: forms still work | GA not configured → no banner, no tag, forms work (all scenarios). GA test build: "Decline" → no Google request, form accepted | PASS | S13; `consent.json` (decline) | GA4 ID (D4) optional |
| 14 | Analytics allowed: events verified, refresh does not double count | Test build with `G-TEST00000` (Google requests intercepted): consent default ads denied; config without automatic page_view; page_view location = path + UTM/gclid only (injected `email=` removed); `generate_lead` exactly once; after reload 0; no name, e-mail or request ID in the data layer | PASS | `consent.json` (allow); S13 event log | GA4 ID (D4) |
| 15 | Widths 320 / 390 / 768 / 1024 / 1440 × EN / RU / ZH | 9 pages × 5 widths × 3 locales = 135 page views, horizontal overflow 0 | PASS | S15 + screenshots | — |
| 16 | 200 % zoom | 640 px CSS viewport at DPR 2 on 5 pages: no overflow | PASS | S16 | — |
| 17 | Reduced motion | `prefers-reduced-motion: reduce` → animation none, scroll-behavior auto | PASS | S16 | — |
| 18 | Keyboard, Escape, focus return, sticky header | Desktop menu Enter/Escape → focus back on the group button; mobile menu Escape → focus back on the toggle; empty submit → error summary, focus on the first invalid field, fully visible below the sticky header | PASS | S14, screenshot `form-validation-errors-mobile` | — |
| 19 | Accessibility scan | axe-core WCAG 2.0/2.1/2.2 A–AA: 18 pages × 2 widths + validation-error state × 2 = 38 runs, 0 violations; visual review of multi-choice fields (checkbox size fixed) | PASS | `axe.json`, screenshots | — |
| 20 | Crawl, SEO basics | 96 localized paths 200; unknown page 404; `lang`, canonical, hreflang (en, ru, zh-CN, x-default) on every page; sitemap 96 URLs incl. `/choose`; robots disallows `/api/`; titles without "MAXIMUS GPS" duplication; Organization JSON-LD factual only | PASS | `scripts/crawl.mjs` output | — |
| 21 | Forbidden content | No store links (maximussports.ae), no confidential figures, formula, counterparty names, "Zero Weight Tolerance", "240–340 g", 50 % model, wallets or payouts in public sources or rendered text | PASS | `npm run check:data`, crawl | — |
| 22 | Product data integrity | Matrices 32 / 33 / 29; SPIN "requested architecture" with balance not provided; GREAT/POWER balance calculated; swingweight/stiffness not provided; P2.5/P1.5/P0.5 values; SST 50 in² / 27″ / 12×14 / 270-330 · 285-325 · 300-325 · 400-320 | PASS | `npm run check:data` | — |
| 23 | Locale parity | EN/RU/ZH identical key structure, technical values unchanged | PASS | `npm run check:i18n` | — |
| 24 | Intake unit / integration | 24 tests: schema, idempotency, rate limit (429), honeypot / too fast, no store → 503, store errors, logs without content, retry schedule, not-configured queue, real Redis via REST, delivery lock, webhook HMAC, SMTP TLS | PASS | `npm run test:unit` | — |
| 25 | Security headers and secrets | Secrets only in server env; `/api/leads` JSON only, ≤ 32 KB, no GET; register 404 without password, Basic auth, rate-limited failures, no-store, noindex, CSP `default-src 'none'`, same-origin POST; no public read of requests | PASS | route code + S12 | LEADS_ADMIN_PASSWORD (D3) |
| 26 | Performance (lab) | Lighthouse 13.5 mobile, simulated throttling (RTT 150 ms, 1.6 Mbps, CPU ×4, Moto G Power 412×823): Performance 94–99; A11y / BP / SEO 100; CLS 0; TBT 94–138 ms; LCP /ru 1.9 s, /en/choose 2.0 s, /en/build 2.2 s — **/en 3.0 s, /zh 2.8 s, /en/racquets/power 2.9 s exceed the 2.5 s target in this lab profile** (LCP element = product photograph) | PARTIAL | `lighthouse.json` | Field data: none exists yet (no CrUX, no RUM). INP cannot be measured in the lab |
| 27 | Mainland China | Not tested (no mainland network, no WeChat in-app test) | NOT RUN | — | D5 |
| 28 | Physical devices | Not tested (emulation only) | NOT RUN | — | Owner's device check on the preview |
